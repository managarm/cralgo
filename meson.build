project('cralgo', 'cpp')

# We provide a "sources" variable such that other projects
# can simply embed cralgo by compiling these sources.

sources = files(
	'src/aes.cpp',
	'src/sha2_32.cpp',
)

includes = include_directories('include/')
